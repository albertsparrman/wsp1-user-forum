{% extends "layout.njk" %}
{% block  content %}
<main class="container mt-3">
    <h1>{{ title }}</h1>
    <a href="/new">Nytt inlägg</a>
    <div class="sak">
    {% for row in rows %}
        <div class="beans">
            <div class="top">
                <p class="username">
                    @{{ row.name }}
                </p>
                <h2>
                    {{ row.title }}
                </h2>
            </div>
            <p>
                {{ row.content }}
            </p>
        </div>
    {% endfor %}
</div>
</main>
{% endblock %}